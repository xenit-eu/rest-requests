configure(subprojects) {
    plugins.withType(JavaPlugin) {
//        apply from: "${rootDir}/gradle/publish.gradle"
//
        java {
            withSourcesJar()
            withJavadocJar()
        }
//
        repositories {
            mavenCentral()
        }
    }
}
